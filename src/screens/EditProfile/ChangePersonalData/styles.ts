import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabView: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingBottom: 20,
  },

  changeDataInputsContainer: {
    marginVertical: 20,
    marginHorizontal: 30,
    justifyContent: 'space-between',
    height: 130,
  },
});

export default styles;
